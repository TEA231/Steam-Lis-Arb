services: 
  db: 
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    volumes:
      - cs_skins_dataset:/var/lib/postgresql/data
    env_file:
      - .env.local

volumes:
  cs_skins_dataset:
    external: true